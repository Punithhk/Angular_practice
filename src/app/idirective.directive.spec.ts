import { IdirectiveDirective } from './idirective.directive';

describe('IdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
